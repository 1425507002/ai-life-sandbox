# 开源参考项目复测与借鉴矩阵

> A 阶段记录（2026-09-06）。本文件只记录公开仓库的只读复测与设计借鉴，不把第三方源码、资源或密钥复制进本项目。

## 当前仓库复测

| 项目 | 复测提交 | 结果 | 结论 |
| --- | --- | --- | --- |
| Covel | `11fb4243668b6d06a23d7fe8fb24487475bbc416` | `events 46`、`memory 63`、`tools 122`、`plugin-loader 122` 全部通过 | 核心事件、记忆、工具和插件边界可独立运行；完整工作区在 Windows 上被 `better-sqlite3` 原生编译依赖阻断，缺少 Visual Studio C++ 构建链，不能视为源码测试失败 |
| Aikami | `30c5cd83486cb4ec6b2d6db91c1b4ab2eb77e56a` | AI Gateway `70/70` 通过；客户端 `1953 pass`、`1 skip`、`2 todo`、`41 fail`，并有多处未处理模块导出错误 | Gateway 的 provider、取消、错误归一化、结构化输出和非 JSON 兜底值得借鉴；客户端当前最新快照存在资源目录/服务导出/测试环境一致性问题，不能作为整体质量基线 |

Aikami 的客户端脚本直接调用 `bunx`，本机 Bun 1.4.0 未提供该命令；使用等价的 `bun x` 完成了测试启动。测试过程中还出现 SvelteKit 配置别名提示和 Bun 的目录句柄提示，这些属于上游/运行时兼容性信号。

## 其他已完成的只读复测结论

- RPGForge：独立 PostgreSQL 测试库的核心测试已通过；完整迁移需要 `pgvector`，因此不在本机补装外部插件。可借鉴状态 Delta、进度存档、trace/usage/latency 记录和记忆压缩接口。
- Project Lunar：事件存储和分层记忆思路有参考价值；此前复测复现了基于相同时间戳的游标丢事件风险。若借鉴，必须使用单调递增序列而不是时间戳作为事件游标。
- Quilltale：可借鉴世界状态、已知地图和 NPC 记忆的产品概念；未发现明确代码许可证，因此只吸收公开设计思想，不直接搬运实现。

## 允许借鉴的方向

1. **确定性状态优先**：规则引擎先结算，AI 只负责解析意图、生成叙事和候选选项；关键状态只能由本地校验后的 Delta 修改。
2. **事件审计**：为每次行动保存输入、结果、状态差异和单调序号，支持履历查看、回放和故障定位。
3. **记忆分层**：固定事实、关系/地点摘要、近期事件和当前行动分开管理；按预算压缩，不把完整历史无限拼入请求。
4. **AI Gateway 边界**：统一 provider、超时、取消、HTTP 错误、结构化响应失败和离线回退，且不把 API Key 写入日志或存档。
5. **数据包校验**：剧本、地图、NPC 和 UI 扩展进入运行时前先做 schema 校验；缺资源时给出可理解的降级提示。

## 明确不直接搬运的内容

- 不复制 Aikami 的图片、音频、字体和其他资产；其仓库存在独立资产许可文件。
- 不复制无明确许可证项目的源码或资源。
- 不在第一阶段引入 RPGForge 的完整后端数据库、Neo4j/pgvector 或多人服务。
- 不复制第三方项目的品牌、既有 IP 剧本或具体文案。

## A 阶段输出与后续输入

- A 阶段完成最新仓库指针校准、核心测试复测、失败边界记录和借鉴矩阵。
- B 阶段优先处理本项目 P0：重启初始化、年龄/地图/NPC 可见性、重复选项、存档导入导出、AI 超时和错误反馈。
- C 阶段实现本项目自己的单调事件序号、状态 Delta 和回放审计。
- D 阶段实现受预算约束的分层记忆压缩与压力测试。
- E 阶段完善 AI Gateway 的结构化输出、错误归一化、耗时/用量观测和离线降级。
- F 阶段执行发布前许可证、密钥、构建、响应式浏览器和全量回归审计。
