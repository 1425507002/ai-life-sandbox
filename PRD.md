# AI 人生模拟器（AI Life Worlds）产品需求文档

> 文档版本：V0.6
>
> 更新日期：2026-09-04
>
> 产品状态：阶段 3 地图发现、年龄阶段与关系链路已实现并验证，阶段 4 长期记忆压缩与上下文压力测试进行中
>
> 文档用途：作为产品、设计、开发、测试和后续商业验证的共同基线

## 1. 产品概述

### 1.1 产品名称

暂定名称：AI 人生模拟器 / AI Life Worlds。

### 1.2 产品定位

一个可以加载不同剧本的、带真实规则和长期因果关系的 AI 人生世界。

产品不是普通的 AI 聊天，也不是只有固定选项的传统文字冒险，而是让玩家在一个持续运行的世界中生活、选择、承担成本，并观察人物、关系、资源和事件如何变化。

核心表达：

> 玩家决定做什么，规则决定能否做到，世界决定会留下什么。

### 1.3 参考边界

项目参考了 AI RPG、人生模拟器、开放世界叙事和用户提供的 UI 案例，包括纸张、手账、状态卡、剧本切换和自由行动等思路。

参考内容只用于提取交互规律和产品经验，不复制既有产品的视觉、文案、角色、世界观或 IP 内容。首发内容采用原创小型西幻世界。

### 1.4 产品层级与术语边界

本项目必须区分“剧本世界”“地图”“人生存档”三个不同层级。它们不能因为都出现在同一个选择界面中，就被统称为“剧本”。

| 层级 | 定义 | 示例 | 用户操作 |
|---|---|---|---|
| 剧本世界 | 一套完整的世界观、规则、种族/身份、职业生态、事件体系和长期因果 | 《西方世界的人生》、未来的哈利波特世界、火影忍者世界、斗破苍穹世界 | 切换剧本、导入剧本 |
| 地图/地区 | 某个剧本世界内部的大陆、区域、城镇、村庄、丛林、山脉和具体地点 | 《西方世界的人生》中的晨雾镇、灰潮港 | 选择出生地、移动、旅行、迁居 |
| 人生存档 | 玩家在某个剧本世界中的一条具体人生时间线，包括角色、关系、资源、事件和履历 | 在晨雾镇开始的一生、在灰潮港重新开始的一生 | 新的人生、继续游戏、保存、导入、回档 |
| UI 主题 | 不属于世界事实的视觉和交互偏好 | 纸页手账、暮色酒馆、星图秘典 | 设置中切换主题 |

因此：晨雾镇和灰潮港是同一个《西方世界的人生》剧本中的地图/起始地区，不是两个剧本；在同一剧本下重新选择地区或重新开始人生，也不应被称为“切换剧本”。

## 2. 产品目标

### 2.1 核心目标

验证玩家是否愿意在一个具有持续状态、人物关系和长期后果的 AI 世界中反复生活，而不是只进行一次对话。

### 2.2 第一阶段目标

- 在手机和电脑上提供一致的可玩体验。
- 使用同一套 UI 加载多个不同剧本。
- 玩家可以通过行动选择推进世界。
- 每次行动都有明确的条件、成本、风险和结果。
- 行动后产生新的行动入口，避免重复选项。
- 无 API 时仍可通过本地确定性规则继续游玩。
- 完成存档、刷新、导入、导出和剧本切换。

### 2.3 不在当前范围内

- 账号、云同步和跨设备登录。
- 多人联机和实时协作。
- 复杂战斗、完整经济系统和大地图模拟。
- 首发使用《斗破苍穹》《斗罗大陆》《宝可梦》等既有 IP。
- 完整的在线剧本市场。
- 让 AI 直接决定或写入关键游戏事实。

## 3. 目标用户

### 3.1 主要用户

- 喜欢文字冒险、人生模拟、开放世界和角色扮演的玩家。
- 喜欢观察 NPC 自主生活和长期关系变化的玩家。
- 不喜欢复杂操作，但希望拥有较高叙事自由度的玩家。

### 3.2 创作者用户

- 想制作原创世界、人生剧本和互动叙事的作者。
- 想通过配置规则、人物、地点和事件生成可玩内容的独立开发者。

### 3.3 用户痛点

- 普通 AI 对话容易遗忘设定，状态不稳定。
- 传统文字游戏选项固定，玩家自由度不足。
- 纯 AI 生成容易出现资源、时间和事实前后矛盾。
- 很多游戏只提供剧情推进，缺少“生活正在继续”的感觉。

## 4. 核心设计原则

1. **规则优先**：时间、资源、关系、地点、物品和事实由确定性规则掌握。
2. **AI 辅助而非越权**：AI 可以解析行动、提出候选和润色叙事，但不能直接修改关键状态。
3. **选择优先**：默认让玩家通过行动卡推进，不强迫玩家输入长文本。
4. **自由仍然存在**：自由输入作为可选模式保留，服务于喜欢探索边界的玩家。
5. **世界持续运行**：玩家没有行动时，暂不模拟大量后台世界；每次行动后必须留下可追踪的变化。
6. **剧本可替换**：剧本改变完整世界内容、规则和生态，但不复制整套应用 UI；地图属于剧本内部结构。
7. **本地优先**：第一阶段不依赖账号或云服务，用户可以导出自己的存档。
8. **原创优先**：商业验证使用原创世界，规避既有 IP 授权风险。
9. **主题可选**：视觉风格和部分交互偏好由玩家选择，但不改变规则、状态和结果的公平性。

### 4.1 主题系统设计原则

- 主体信息架构、核心组件、存档结构和游戏规则保持不变。
- 主题通过设计令牌、组件变体和受控交互策略实现，不复制四套页面。
- 剧本主题与玩家主题偏好分层：剧本可以提供推荐氛围，玩家选择优先；用户明确选择的主题不会被剧本静默覆盖。
- 主题切换必须即时生效，并保留当前剧本、存档、行动历史和模型设置。
- 主题不能改变行动成本、条件判定、随机结果、NPC 事实或 AI 权限。
- 动效默认克制，支持减少动效和低性能设备模式；“呼吸”只用于等待、提示和可选氛围反馈，不能影响操作时序。

## 5. 产品形态与技术基线

### 5.1 首发形态

- React + Vite + TypeScript。
- 响应式 Web/PWA。
- 手机和桌面共用一套前端代码。
- 单人、本地优先、JSON 存档导出/导入。
- 后续根据用户验证结果评估 Capacitor 移动端封装和 Tauri 桌面端封装。

### 5.2 当前技术栈

- React：界面和交互。
- Zustand：运行时状态管理。
- IndexedDB：本地存档、设置和剧本保存。
- Vite：开发和生产构建。
- PWA：离线静态壳和资源缓存。
- OpenAI-compatible API：可选的叙事生成适配接口。
- Vitest：规则和行动生成单元测试。
- Playwright：浏览器交互与响应式验收。

## 6. 核心游戏循环

```text
进入剧本
  ↓
查看当前场景、人物和世界状态
  ↓
选择行动卡，或在自由行动模式输入行动
  ↓
规则层检查条件、资源、时间、风险和地点
  ↓
结算成功 / 部分完成 / 拒绝 / 失败 / 待确认
  ↓
写入时间、资源、地点、关系、物品、事实和履历
  ↓
生成下一轮不重复的行动建议
  ↓
玩家继续生活
```

### 6.1 行动模式

| 模式 | 默认状态 | 行动入口 | 适用用户 |
|---|---|---|---|
| 选择优先 | 是 | 展示当前最合适的 3 个行动，隐藏输入框 | 想快速游玩的用户 |
| 丰富建议 | 否 | 展示完整行动池，按进度轮换顺序，可手动换顺序 | 想比较更多方向的用户 |
| 自由行动 | 否 | 展示行动卡，同时保留自由输入 | 喜欢主动探索的用户 |

模式设置保存在本地，并必须实际影响行动入口数量、排序或输入能力。

## 7. 功能需求

优先级说明：P0 为第一阶段必须完成，P1 为第二阶段优先，P2 为后续扩展。

### 7.1 应用壳与响应式 UI（P0）

- 桌面端提供侧栏导航。
- 手机端提供底部导航和可展开侧栏。
- 主体 UI 保持纸张、手账、自然色的原创视觉系统。
- UI 必须适配剧本主题色。
- 页面包含当前场景、角色、人物、履历、地图和设置。
- 行动入口在选择优先模式下首屏可见。
- 输入反馈、行动结果和资源变化必须清晰可见。
- 页面不应被大量永久信息卡片压缩成后台仪表盘。
- 支持主题系统，在不改变主体信息架构的情况下切换 3～4 套视觉风格。
- 主题切换至少覆盖颜色、字体层级、卡片/面板外观、图标语气、动效强度和导航密度。
- 主题可以改变交互表现（例如卡片展开、抽屉、分段导航和呼吸提示），但不能改变核心操作语义。

状态：已完成基础版本，持续优化中。

### 7.2 剧本加载与切换（P0）

- 支持内置剧本世界。
- 支持导入 `.aiworld.json` 剧本包。
- 支持在不替换主体 UI 的情况下切换真正不同的剧本世界。
- 剧本可定义标题、描述、世界规则、人物生态、地图层级、职业约束、行动、事件和初始状态。
- 剧本选择列表只展示世界级内容，不得把同一剧本内的城镇、区域或地点伪装成多个剧本。
- 同一剧本下的地图选择应放在角色创建、出生地选择、地图或人生重开流程中。
- 当前原创首发剧本统一命名为《西方世界的人生》；晨雾镇、灰潮港作为其内部地区/起始地图。
- 无效剧本必须被拒绝，并给出可理解的错误提示。

状态：基础加载能力已完成；世界级剧本与地图级内容的边界重构安排在阶段 3A。当前原型中的两个原创入口应在后续实现中合并为一个《西方世界的人生》剧本，并保留两个地图样例。

地图发现规则：

- 出生地可以由玩家选择，但新人生第一次打开地图时，只能看到当前出生地对应的一个已知地点。
- 地图中的 `available` 表示规则上是否可用，`discovered` 表示玩家是否已经知道；两者不能混用。
- 行动、探索、NPC 介绍、传闻和延迟事件可以通过剧本规则显式揭示已有地点；每次行动默认最多揭示一个地点。
- 未知地点不应出现在地图标记、已知地点列表、行动文案或 AI 的普通记忆上下文中；地图界面可以显示未知区域数量和模糊提示。
- 地点必须先存在于剧本地图目录，运行时不得由叙事文本直接创建地点。AI 只能提出已有地点 ID，最终由 schema 和确定性引擎校验、写入 `discovered`。
- 地图发现属于当前人生存档；重开人生重新从出生地开始，切换同一剧本下的另一条人生不共享探索进度。

### 7.3 角色创建（P0/P1）

当前版本补充：

- 玩家姓名。
- 年龄或人生阶段。
- 身份和职业。
- 初始特质。
- 初始资源或背景选项。
- 剧本可控制字段，不允许玩家编辑规则不允许的字段。

角色创建结果需要写入当前剧本存档，并影响初始行动、关系、资源或叙事。

状态：已完成当前阶段范围。剧本可声明角色创建字段，玩家可在角色页编辑姓名、年龄、身份、职业、心情和特质；首次进入强制向导留待阶段 3。

年龄阶段约束：

- 新人生必须从第 1 日、清晨 07:00、空履历和空待处理事件开始，不得复用演示种子的日期、资源、事实或历史。
- 支持婴儿期、童年期、少年期、成年期和老年期；选择阶段后年龄会落在对应区间，身份和职业从该阶段与地图共同允许的集合中生成。
- 婴儿、儿童、少年和老年使用专属行动池；成年地图行动不会被展示，也不能通过自由输入绕过年龄规则。
- 角色编辑修改年龄时自动重新计算年龄阶段，并重新校正身份和职业。

### 7.4 当前场景（P0）

- 显示当前地点、时间、季节、天气和氛围。
- 显示当前叙事内容。
- 显示“眼下的线索”或当前关注点。
- 显示本轮行动入口和每个行动的时间、精力、金钱、风险。
- 行动执行后显示结果状态和状态差异。

状态：已完成基础版本。

### 7.5 行动生成与去重（P1）

本地生成器必须在没有 AI 的情况下工作：

- 根据当前剧本行动池生成候选行动。
- 根据当前时间、地点、资源和最近行动调整候选。
- 记录每次已执行行动的 `actionId`。
- 默认排除最近一次已完成的规则行动。
- 为同一规则生成不同标题、描述或关注点的变体。
- 候选行动的显示 ID、规则 ID、标题和实际结算逻辑必须可追踪。
- 当候选不足时，可以回收旧规则，但必须使用新的变体并明确避免标题重复。

AI 生成接入后：

```text
剧本规则与当前状态
  ↓
AI 提供候选行动
  ↓
schema 校验与安全过滤
  ↓
重复检测与规则映射
  ↓
确定性规则结算
  ↓
生成叙事
```

状态：已完成。无 API 时使用本地变体生成；配置 AI 后由 AI 仅提供候选行动，候选经过 schema 校验、规则 ID 白名单和去重后才进入界面，不直接修改事实状态。

### 7.6 确定性行动结算（P0）

- 检查金钱、精力、时间、地点和前置条件。
- 成功行动推进世界状态。
- 部分完成允许产生收益和代价并存的结果。
- 条件不足时拒绝行动，不改变世界状态。
- 未识别的自由行动进入待确认状态，不得直接写入关键事实。
- 每次结算产生可读取的状态 diff。
- 规则层独立于 AI 叙事层。

### 7.7 NPC 与关系（P0/P1）

- 显示 NPC 姓名、身份、状态、摘要和关系值。
- 关系值只能由行动规则或明确事件改变。
- NPC 需要保留最近互动记录。
- 后续支持 NPC 自主事件和关系阈值事件。

状态：已完成当前阶段范围。NPC 支持按剧本 schedule 自主推进状态、互动摘要和公共消息；更复杂的关系阈值事件链留待后续阶段。

### 7.8 时间、事件与履历（P0/P1）

- 行动消耗明确时间。
- 世界时间持续推进。
- 履历按时间倒序保存。
- 每条履历包含行动 ID、结果、文本和标签。
- 后续支持延迟事件、条件事件和事件链。

突发事件框架：

- 已结算行动后，AI 可以按剧本配置低频提出一件小型突发事件候选，也可以返回空候选。
- 候选只允许包含标题、正文、类型、标签、延迟轮次和已存在 NPC 的小幅关系变化，不允许直接写入金钱、物品、健康、时间或事实。
- 本地校验负责长度、类型、标签、NPC 存在性、关系变化范围、重复和待处理事件上限；通过后才排入延迟事件队列。
- 事件到期时仍由确定性引擎执行，写入履历并按已验证字段更新关系；AI 服务不可用时不影响正常行动。

状态：年龄阶段初始化、年龄行动池和突发事件候选框架已完成；复杂事件链和更丰富的事件条件留待后续阶段。

### 7.9 存档与数据迁移（P0）

- IndexedDB 保存运行时状态。
- 页面刷新后状态保持。
- 支持全部世界存档导出。
- 支持存档导入。
- 导入后状态和剧本一致。
- 未来版本必须保留存档版本号和迁移策略。

### 7.10 AI 配置与错误处理（P0/P1）

- 支持 OpenAI-compatible Endpoint、Model 和 API Key。
- API Key 只保存在本地原型环境，不由前端生产环境直接长期暴露。
- 未配置 API 时使用本地确定性模拟。
- 请求失败时显示可理解的错误和重试方式。
- 后续支持模型等级、调用额度、缓存和 BYOK。

### 7.11 PWA（P0）

- 支持安装为 Web App。
- 静态资源支持离线缓存。
- 离线时可以打开应用并使用本地规则。
- AI 请求失败不应破坏本地游戏状态。

### 7.12 主题与个性化交互（P1）

玩家可以在设置中选择并预览主题。首批规划四个主题，名称和最终视觉稿在实现前确认：

| 主题 | 视觉方向 | 适合的交互表现 |
|---|---|---|
| 纸页手账 | 延续当前纸张、手账、自然色和生活记录感 | 卡片叠放、清晰行动按钮、轻量呼吸提示 |
| 暮色酒馆 | 深色木质、烛火、铜色和低照度叙事感 | 抽屉式信息、聚焦当前行动、弱化背景信息 |
| 星图秘典 | 深蓝、星图、卷轴和探索感 | 分段导航、线索高亮、地图/履历联动 |
| 清透旅记 | 浅色、留白、旅行记录和轻量工具感 | 紧凑列表、快速切换、减少装饰动效 |

功能要求：

- 设置页展示四个主题的缩略预览、适用场景和动效说明。
- 支持一键切换、恢复默认和“跟随剧本推荐”三种方式。
- 支持独立设置动效强度：标准、轻量、减少动效。
- 支持独立设置内容密度：舒适、紧凑；不得隐藏核心状态和行动入口。
- 主题偏好保存在本地，并在刷新、导入存档和切换剧本后保持。
- 主题切换需要覆盖桌面和手机布局，不能造成横向溢出、行动入口消失或文字对比度不足。
- 首批只做预设主题，不开放任意颜色编辑器；避免把主题系统变成高维护的页面分叉。

状态：新增需求，安排在阶段 3，优先于剧本编辑器。

## 8. UI 信息架构

### 8.1 当前场景

信息顺序：

1. 当前世界标题和时间。
2. 场景叙事。
3. 当前线索。
4. 行动入口。
5. 行动结果。
6. 辅助世界状态。

移动端选择优先模式将行动入口前置，保证玩家在首屏看到可操作内容；自由行动模式保留叙事优先和底部输入位。

### 8.2 角色

- 当前身份。
- 职业和年龄。
- 健康、精力、金钱、声望。
- 特质。
- 随身物品。

### 8.3 人物

- NPC 卡片。
- 关系值。
- 当前状态。
- 最近互动。

### 8.4 履历

- 时间线。
- 行动结果标签。
- 状态变化。
- 事件标签。

### 8.5 地图

- 已知地点。
- 当前地点。
- 地点类型和距离。
- 未探索区域不自动展示。
- 新人生从出生地对应的一个已知地点开始，地图显示“已知数量 / 地点目录数量”和未知区域提示。
- 真实抵达、探索、NPC 介绍、传闻或延迟事件可以逐步解锁地点；解锁来源和轮次应可追踪。
- 地图 UI 只能渲染 `discovered !== false` 的地点，不能因加载地图而把整个地图目录展示给玩家。
- 地图展示应标明所属剧本世界、地区层级和可用职业/行动范围。
- 地图选择、出生地选择和剧本切换必须使用不同的文案和入口。
- 地图变化可以发生在同一人生中；地图变化不等于剧本变化。

### 8.6 剧本与人生管理

- “切换剧本”只用于切换完整的世界包，例如从《西方世界的人生》切换到另一个独立世界。
- “选择地图/出生地”用于同一剧本内部选择起始地区，不清空已有剧本定义。
- “重开人生”用于在当前剧本中创建一条新的人生存档，可以重新选择姓名、年龄阶段、身份、职业、关系和出生地。
- “继续人生”用于打开已有的人生存档；“回档”用于回到该人生的历史节点。
- 同一剧本可以拥有多个独立人生存档，存档之间不得相互覆盖。
- 切换剧本不删除原剧本的人生存档；返回原剧本时可以继续已有存档或创建新人生。
- 界面必须分别使用“切换剧本”“选择出生地”“开始新人生”“继续人生”等文案，避免用户误解。

### 8.7 设置

- 行动呈现方式。
- 主题选择、主题预览、跟随剧本推荐。
- 动效强度和内容密度。
- 模型连接。
- 存档管理。
- 剧本导入。
- 当前剧本信息。

## 9. 数据与规则模型

### 9.1 核心状态

`GameState` 至少包含：

- `player`：角色基础信息、健康、精力、金钱、声望和特质。
- `world`：日期、时间、季节、天气、地点、氛围、叙事、当前线索和公共消息。
- `scriptId`：当前所属剧本世界 ID；地图名称不能替代此字段。
- `npcs`：人物状态和关系。
- `locations`：已知地点。
- `suggestedActions`：当前行动候选。
- `history`：履历和行动结果。
- `inventory`：物品。
- `knownFacts`：玩家已经知道的事实。
- `turn`：行动轮次。

用户界面偏好不属于 `GameState` 的游戏事实，单独保存在本地设置中：

```ts
{
  themeId: 'paper-journal' | 'twilight-tavern' | 'star-atlas' | 'clear-travel-log' | 'script-recommended'
  motionLevel: 'standard' | 'light' | 'reduced'
  density: 'comfortable' | 'compact'
}
```

主题设置可以随设备保留，也可以随存档导入导出，但无论采用哪种产品实现，都不能改变游戏规则、行动结果或世界事实。

### 9.2 行动对象

每个行动至少包含：

```ts
{
  id: string
  ruleId?: string
  title: string
  description: string
  location: string
  timeCost: number
  moneyCost: number
  staminaCost: number
  risk: string
  tone: string
}
```

其中：

- `id` 用于追踪当前显示的行动变体。
- `ruleId` 用于映射确定性结算逻辑。
- 成本和风险由规则层读取，不能只由叙事文本表达。

### 9.3 剧本包

当前 `.aiworld.json` 顶层包含：

- `manifest`
- `theme`
- `world`

地图建议使用分层结构表达，而不是把地区名称直接拼接进剧本标题：

```ts
{
  map: {
    continents: [],
    regions: [],
    settlements: [],
    locations: [],
    startOptions: []
  }
}
```

其中 `startOptions` 只描述该剧本允许的出生地/开局地区；职业、身份和行动的可用性由地图标签与规则共同判断。

后续扩展：

- `characterCreation`
- `actionPool`
- `rules`
- `conditions`
- `events`
- `npcSchedules`
- `uiExtensions`

剧本扩展必须保持向后兼容，并提供版本号和校验错误信息。

## 10. AI 使用边界

### 10.1 允许 AI 做的事

- 将自由文本解析为候选行动意图。
- 生成行动候选文案。
- 根据已经结算的状态 diff 生成叙事。
- 生成 NPC 对话和非事实性氛围描写。
- 给剧本作者提供内容草稿。

### 10.2 不允许 AI 直接做的事

- 直接增加金钱、物品、生命、关系或声望。
- 直接改变时间、地点或已知事实。
- 直接跳过行动条件。
- 直接创建未经验证的 NPC、地点或事件；涉及地图扩张时，只能从剧本提供的未发现地点目录中提出已有 `locationId`，不得提交新地点对象。
- 用一段叙事文本覆盖完整 `GameState`。

### 10.3 失败降级

当 AI 不可用、超时、额度不足或返回格式错误时：

- 保留确定性规则结算结果。
- 使用本地默认叙事。
- 显示模型请求失败，但不回滚已经合法结算的状态。
- 允许玩家重试叙事请求。

## 11. 开放核心与商业路线

### 11.1 开源部分

默认采用 Apache-2.0：

- 通用 UI 壳。
- 剧本读取和校验。
- 状态模型。
- 行动规划接口。
- 确定性规则引擎。
- 本地存档。
- API 适配接口。
- 示例原创剧本。

### 11.2 可商业化部分

- 官方 AI 托管。
- 跨设备同步。
- 剧本市场与分成。
- 高级剧本编辑器。
- 官方原创剧本包。
- 高级模型和长期记忆额度。
- 面向创作者和工作室的技术服务。
- 白标部署。

### 11.3 商业验证原则

第一阶段不急于收费或大规模投入，先验证：

- 玩家是否会持续经营同一个人生。
- 玩家是否会尝试多个剧本。
- 玩家是否愿意导入或创建剧本。
- 玩家愿意为什么功能付费。
- 每位活跃用户的模型成本是否可控。

原则上不采用影响游戏公平性的付费数值和强制广告。

## 12. 关键指标

### 12.1 游戏体验指标

- 首次进入到角色/剧本开始的完成率。
- 首次行动完成率。
- 首次行动后的第二次行动率。
- 单次游戏时长。
- 每个会话平均行动数。
- 次日和第七日回访率。
- 选择优先模式与自由行动模式的使用比例。
- 重复选项投诉率。

### 12.2 内容与创作指标

- 导入剧本数量。
- 用户创建剧本数量。
- 单个剧本平均游玩行动数。
- 用户主动分享剧本数量。

### 12.3 技术指标

- 行动规则错误率。
- AI 请求失败率。
- 状态 diff 校验失败率。
- 存档恢复失败率。
- 页面首屏加载时间。
- 移动端主要交互成功率。

## 13. 阶段计划

### 阶段 0：调研与决策（已完成）

- 调研 AI RPG、叙事引擎、NPC 模拟和剧本系统。
- 确认开放核心 + 官方服务路线。
- 确认原创剧本优先。
- 确认规则引擎优先于 AI 叙事。

### 阶段 1：通用 UI 与可玩原型（已完成）

- 响应式 Web/PWA 应用壳。
- 当前场景、角色、人物、履历、地图和设置。
- 两个原创测试剧本。
- 剧本切换和主题替换。
- 选择优先、丰富建议、自由行动三档模式。
- 确定性行动结算。
- 行动结果和状态差异反馈。
- 本地存档、导入导出和 PWA。
- 桌面和移动端浏览器验证。

### 阶段 2：行动生成与规则扩展（已完成）

当前已完成：

- 本地行动变体生成器。
- 最近行动记录。
- 最近行动排除。
- 行动 ID 与规则 ID 分离。
- 行动标题和候选去重。
- 更严格的剧本包、行动候选和运行时载荷校验。
- 状态条件 DSL：资源、地点、时间、关系和布尔条件。
- 候选行动的校验、去重、白名单映射和无 API 降级策略。
- AI 候选生成适配器，AI 不直接写入 `GameState`。
- 延迟事件、到期事件履历和 NPC schedule 自主推进。
- 状态 diff、事件日志和桌面/移动端 Playwright 核心流程测试。

验收结果：已通过 `pnpm build`、14/14 Vitest 单元测试、桌面核心流程和移动端布局测试。真实外部 AI 端点因未配置 API Key 未进行在线调用测试，但失败会回退到本地生成。

### 阶段 3：世界层级、剧本边界、主题系统与人生起点约束（已实现）

#### 阶段 3A：世界层级与剧本边界

目标：把完整剧本、内部地图和具体人生存档拆成清晰的产品和数据层级，修正当前将晨雾镇、灰潮港显示成两个剧本的问题。

- 将首发世界统一命名为《西方世界的人生》。
- 将晨雾镇、灰潮港迁移为该剧本内部的地区/起始地图，不再作为两个独立剧本展示。
- 将“切换剧本”“选择出生地/地图”“重开人生”“继续人生”“回档”拆成不同入口和动作。
- 设计 `scriptId`、`mapId`、`lifeId` 的关系，确保同一剧本可以保存多条人生。
- 扩展剧本包地图结构，支持大陆、区域、聚落、地点和起始地区。
- 为地图、职业、身份和行动建立约束关系；不允许把某地区不可用的职业直接展示为可选项。
- 兼容旧存档：旧原型中的地区标识需要映射到新的统一剧本 ID 和地图 ID，不能静默丢失人生数据。
- 验证切换世界不会删除旧人生，切换地图不会错误重置剧本，重开人生不会覆盖旧存档。

阶段 3A 的完成条件：剧本列表只展示世界级剧本；《西方世界的人生》内部可以选择晨雾镇或灰潮港；同一剧本可以创建并保存多条人生；四种操作的界面文案、状态变化和存档影响均可区分。

#### 阶段 3B：主题系统与个性化交互

目标：在保持主体框架不变的前提下，提供 3～4 套可以由玩家主动选择的完整视觉主题，并验证不同主题下的游戏交互仍然一致、清晰、可玩。

- 建立统一的主题令牌：颜色、背景、字体层级、圆角、阴影、边框、间距、图标语气和动效参数。
- 将场景、角色、人物、履历、地图、设置、行动卡和反馈组件改造成主题变体，而不是复制页面。
- 实现四个预设主题：纸页手账、暮色酒馆、星图秘典、清透旅记。
- 实现设置中的主题预览、切换、默认恢复、跟随剧本推荐、动效强度和内容密度。
- 规定剧本推荐主题与玩家手动选择的优先级，并在状态/存档中持久化偏好。
- 为每个主题定义可接受的交互差异：导航形式、信息展开方式、提示动效和视觉层级；核心操作语义保持一致。
- 用桌面、窄手机和宽手机验证主题切换、行动入口、底部导航、抽屉和长文本阅读。
- 修复重开人生复用演示种子的问题，补充年龄阶段初始化、阶段行动池和年龄/地图双重约束。
- 在移动端将新人生与角色弹窗提升到应用壳层，避免被固定底部导航遮挡提交按钮。
- 增加 AI 突发事件候选接口，但坚持由规则引擎校验和结算。

阶段 3B 的完成条件：四个主题都能加载同一剧本并完成一次核心行动循环；切换主题不重置游戏状态、不改变规则结果、不产生布局溢出；关闭动效后仍能理解等待、成功、失败和错误状态。当前已完成：`纸页手账`、`暮色书库`、`远行札记`、`港口明信片`，并通过桌面/移动 Playwright 验收。年龄阶段与移动弹窗修复也已通过桌面和窄手机验收。

### 阶段 4：长期记忆压缩与上下文压力测试（进行中）

- 保留完整权威状态和玩家可见履历，不用摘要覆盖事实。
- 为模型请求生成有上限的 `MemoryPacket`：当前状态、固定事实、待处理线索、最近 8 条履历。
- 对较早履历生成确定性的本地摘要，限制摘要长度，防止上下文随人生长度线性增长。
- AI 只能润色叙事或提供候选，规则引擎继续负责事实、资源、时间和关系变化。
- 以可配置模型上下文窗口进行 2 倍、3 倍及以上历史压力测试，记录压缩包大小、关键信息保留率和生成失败情况。

### 阶段 5：剧本创作能力（计划）

- 角色创建配置。
- 剧本编辑器。
- 事件和规则可视化。
- 剧本预览和校验。
- 创作者导入、导出和版本管理。

### 阶段 6：封闭测试与商业验证（计划）

- 小范围邀请测试。
- 记录留存、行动数、成本和错误率。
- 访谈玩家和剧本作者。
- 验证托管 AI、同步、内容包和创作工具的付费意愿。

## 14. 验收标准

### 14.1 第一阶段验收

- 手机和桌面均可完整进入并游玩。
- 剧本切换不破坏主体 UI。
- 默认选择优先模式不依赖输入框即可推进。
- 自由行动模式可以恢复输入框。
- 不合理行动被规则层拒绝。
- 合法行动会消耗正确的时间和资源。
- AI 不可用时仍可继续运行。
- 页面刷新后存档仍存在。
- 导出和导入后状态一致。
- 每次行动都有可追踪的履历和结果。
- 成功、部分完成、拒绝、未知行动均有可理解反馈。
- 移动端底部导航、行动入口和安全区域正常。

### 14.2 第二阶段验收

- 连续行动后不会立即出现相同的行动标题。
- 行动候选 ID 唯一且可映射到规则 ID。
- 最近行动被记录并参与下一轮生成。
- 本地生成器无 API 也能产生新行动变体。
- AI 候选格式错误时不会污染 `GameState`。
- 条件不足、候选重复和生成失败都有降级方案。
- 规则结算和叙事生成可以独立测试。
- Playwright 覆盖桌面核心流程、手机行动入口、底部导航和横向溢出检查。

### 14.3 主题系统验收

- 至少四个主题可以通过同一设置入口切换，并显示可理解的预览。
- 四个主题均可加载同一剧本，完成角色、场景、行动、结果和存档流程。
- 切换主题不会重置剧本、角色、资源、关系、履历、地图或 API 设置。
- 主题差异可以被用户感知，但核心操作名称、行动语义和规则结果保持一致。
- 主题推荐、手动选择和恢复默认的优先级明确且可重复验证。
- 动效强度为“减少动效”时，不依赖动画才能理解加载、成功、失败、拒绝和错误。
- 桌面、窄手机和宽手机均无横向溢出；行动入口、底部导航和主要状态始终可访问。
- 文本、按钮、状态标签和错误提示在四个主题下均保持足够对比度和可读性。
- 刷新页面、导入存档、切换剧本后，主题偏好按设计规则恢复。

### 14.4 世界层级与剧本切换验收

- 剧本选择列表不会把晨雾镇、灰潮港等同一世界内地区显示为不同剧本。
- 首发世界统一显示为《西方世界的人生》，地区作为地图/出生地选项展示。
- “切换剧本”只会切换世界级规则和内容；“选择地图”只会改变同一剧本内的起始地区或当前地点。
- “重开人生”会创建新的 `lifeId`，允许重新配置角色和起始地图，并保留旧人生存档。
- 同一剧本下的多条人生可以独立保存、继续和回档，不发生互相覆盖。
- 地图、职业、身份和行动的约束可被规则层验证；不可用选项不会伪装成可执行选项。

### 14.5 长期记忆与上下文压力验收

- 完整履历和权威 `GameState` 始终保留，压缩摘要不能替代规则事实。
- AI 请求只接收受控的 `MemoryPacket`，包含当前状态、固定事实、待处理线索和最近 8 条履历。
- 历史长度增加时，`MemoryPacket` 大小保持在明确上限内，不随完整履历线性增长。
- 压缩前后的角色名、地图、关系、物品、资源和未完成线索可被验证保留。
- 模型返回失败、格式错误或超出上下文时，游戏继续使用本地确定性规则和候选。
- 默认按 128K 上下文窗口执行超过 3 倍的本地压力测试；实际接入其他模型时用其官方上下文窗口重跑。
- 从旧原型数据迁移后，原有地区、人物、履历和状态不会因剧本合并而丢失。

## 15. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| AI 生成成本过高 | 商业不可持续 | BYOK、额度、缓存、模型分级、本地降级 |
| AI 修改事实 | 世界状态失真 | 规则层掌握事实，AI 只提交候选或叙事 |
| 行动内容重复 | 玩家快速流失 | actionId、ruleId、变体池、最近行动排除 |
| 剧本质量参差 | 用户体验不稳定 | schema、校验、预览、创作者工具 |
| 既有 IP 授权风险 | 无法商业发布 | 首发使用原创世界，社区剧本自行承担授权责任 |
| 移动端信息过载 | 操作困难 | 选择优先、首屏行动、抽屉和折叠信息层 |
| 本地存档损坏 | 用户数据丢失 | 版本号、导出、导入前校验和迁移策略 |
| 插件职责冲突 | 修改互相覆盖 | 单一主责 Skill、串行调用、项目规则管控 |
| 主题分叉过多 | UI 难维护、行为不一致 | 共享组件和设计令牌，只允许受控主题变体 |
| 主题影响可玩性 | 玩家找不到行动或误解状态 | 核心操作语义固定，使用主题验收矩阵验证 |
| 动效造成疲劳或性能问题 | 移动端体验下降 | 动效强度分级，支持减少动效和低性能模式 |
| 剧本与地图概念混淆 | 用户误解产品结构、存档被错误切换 | 明确 `scriptId`/`mapId`/`lifeId`，分离入口和文案 |
| 合并首发地区导致旧存档丢失 | 用户人生数据不可恢复 | 增加版本迁移、旧 ID 映射和导入前校验 |

## 16. 插件与 Skill 调用规则

- 每个阶段只指定一个主责 Skill。
- 游戏 UI 与交互由 `game-ui-frontend` 主责。
- 游戏体验和浏览器验收由 `game-playtest` 主责。
- React/TypeScript 工程实现遵守 `build-web-apps:react-best-practices`。
- 纯技术故障才使用 `build-web-apps:frontend-testing-debugging`。
- 视觉资产只有在确实需要位图、插画或素材时才调用 `imagegen`。
- 同类型游戏设计、游戏 UI、游戏测试或资源生成工具禁止并行修改同一目标。
- 社区插件必须先核验来源、许可证、安装路径、权限和实际用途，再由用户明确同意后安装。
- 插件和 Skill 只服务开发流程，不进入游戏运行时依赖。
- `game-dev-studio` 已卸载，后续不得重新调用。

## 17. 当前项目文件映射

- [App.tsx](./src/App.tsx)：应用壳、页面和主要交互。
- [actionEngine.ts](./src/engine/actionEngine.ts)：确定性行动结算。
- [actionPlanner.ts](./src/engine/actionPlanner.ts)：行动呈现模式和排序。
- [suggestionEngine.ts](./src/engine/suggestionEngine.ts)：本地行动变体生成。
- [scriptSchema.ts](./src/engine/scriptSchema.ts)：剧本包、行动候选和运行时数据校验。
- [aiProvider.ts](./src/engine/aiProvider.ts)：OpenAI-compatible 叙事与候选行动适配。
- [storage.ts](./src/storage.ts)：IndexedDB、本地存档和运行时载荷校验。
- [store.ts](./src/store.ts)：运行时状态、本地持久化和剧本切换。
- [types.ts](./src/types.ts)：核心数据类型。
- [scripts.ts](./src/data/scripts.ts)：内置原创剧本。
- [AGENTS.md](./AGENTS.md)：项目协作、架构和插件调用规则。
- [e2e/smoke.spec.ts](./e2e/smoke.spec.ts)：桌面和移动端浏览器烟雾测试。
- [playwright.config.mjs](./playwright.config.mjs)：浏览器测试矩阵和本地静态测试服务器配置。

## 18. 待决策事项

以下事项在进入对应开发阶段前再确认：

- 角色创建是首次进入必选流程，还是允许使用剧本默认角色。
- 行动卡是否允许玩家主动锁定或收藏某个方向。
- AI 行动候选由单次请求生成，还是由本地候选池批量缓存。
- 是否在阶段 3 将当前条件 DSL 扩展为完整事件编辑器。
- 首个公开原创剧本的题材、长度和内容分级。
- 四个主题的最终视觉稿、命名和是否允许剧本推荐主题。
- 主题是否允许创作者在 `.aiworld.json` 中声明推荐主题，及其覆盖优先级。
- 《西方世界的人生》的最终世界 ID、地区 ID 和出生地列表。
- 同一剧本多条人生的存档列表、回档和删除交互。
- 封闭测试规模、部署方式和数据采集方案。
