# AI 人生模拟器（AI Life Worlds）产品需求文档

> 文档版本：V0.2
>
> 更新日期：2026-08-31
>
> 产品状态：原型已完成，第二阶段进行中
>
> 文档用途：作为产品、设计、开发、测试和后续商业验证的共同基线

## 1. 产品概述

### 1.1 产品名称

暂定名称：AI 人生模拟器 / AI Life Worlds。

### 1.2 产品定位

一个可以加载不同剧本的、带真实规则和长期因果关系的 AI 人生世界。

产品不是普通的 AI 聊天，也不是只有固定选项的传统文字冒险，而是让玩家在一个持续运行的世界中生活、选择、承担成本，并观察人物、关系、资源和事件如何变化。

核心表达：

> 玩家决定做什么，规则决定能否做到，世界决定会留下什么。

### 1.3 参考边界

项目参考了 AI RPG、人生模拟器、开放世界叙事和用户提供的 UI 案例，包括纸张、手账、状态卡、剧本切换和自由行动等思路。

参考内容只用于提取交互规律和产品经验，不复制既有产品的视觉、文案、角色、世界观或 IP 内容。首发内容采用原创小型西幻世界。

## 2. 产品目标

### 2.1 核心目标

验证玩家是否愿意在一个具有持续状态、人物关系和长期后果的 AI 世界中反复生活，而不是只进行一次对话。

### 2.2 第一阶段目标

- 在手机和电脑上提供一致的可玩体验。
- 使用同一套 UI 加载多个不同剧本。
- 玩家可以通过行动选择推进世界。
- 每次行动都有明确的条件、成本、风险和结果。
- 行动后产生新的行动入口，避免重复选项。
- 无 API 时仍可通过本地确定性规则继续游玩。
- 完成存档、刷新、导入、导出和剧本切换。

### 2.3 不在当前范围内

- 账号、云同步和跨设备登录。
- 多人联机和实时协作。
- 复杂战斗、完整经济系统和大地图模拟。
- 首发使用《斗破苍穹》《斗罗大陆》《宝可梦》等既有 IP。
- 完整的在线剧本市场。
- 让 AI 直接决定或写入关键游戏事实。

## 3. 目标用户

### 3.1 主要用户

- 喜欢文字冒险、人生模拟、开放世界和角色扮演的玩家。
- 喜欢观察 NPC 自主生活和长期关系变化的玩家。
- 不喜欢复杂操作，但希望拥有较高叙事自由度的玩家。

### 3.2 创作者用户

- 想制作原创世界、人生剧本和互动叙事的作者。
- 想通过配置规则、人物、地点和事件生成可玩内容的独立开发者。

### 3.3 用户痛点

- 普通 AI 对话容易遗忘设定，状态不稳定。
- 传统文字游戏选项固定，玩家自由度不足。
- 纯 AI 生成容易出现资源、时间和事实前后矛盾。
- 很多游戏只提供剧情推进，缺少“生活正在继续”的感觉。

## 4. 核心设计原则

1. **规则优先**：时间、资源、关系、地点、物品和事实由确定性规则掌握。
2. **AI 辅助而非越权**：AI 可以解析行动、提出候选和润色叙事，但不能直接修改关键状态。
3. **选择优先**：默认让玩家通过行动卡推进，不强迫玩家输入长文本。
4. **自由仍然存在**：自由输入作为可选模式保留，服务于喜欢探索边界的玩家。
5. **世界持续运行**：玩家没有行动时，暂不模拟大量后台世界；每次行动后必须留下可追踪的变化。
6. **剧本可替换**：剧本改变内容、规则和主题，但不复制整套应用 UI。
7. **本地优先**：第一阶段不依赖账号或云服务，用户可以导出自己的存档。
8. **原创优先**：商业验证使用原创世界，规避既有 IP 授权风险。

## 5. 产品形态与技术基线

### 5.1 首发形态

- React + Vite + TypeScript。
- 响应式 Web/PWA。
- 手机和桌面共用一套前端代码。
- 单人、本地优先、JSON 存档导出/导入。
- 后续根据用户验证结果评估 Capacitor 移动端封装和 Tauri 桌面端封装。

### 5.2 当前技术栈

- React：界面和交互。
- Zustand：运行时状态管理。
- IndexedDB：本地存档、设置和剧本保存。
- Vite：开发和生产构建。
- PWA：离线静态壳和资源缓存。
- OpenAI-compatible API：可选的叙事生成适配接口。
- Vitest：规则和行动生成单元测试。
- Playwright：浏览器交互与响应式验收。

## 6. 核心游戏循环

```text
进入剧本
  ↓
查看当前场景、人物和世界状态
  ↓
选择行动卡，或在自由行动模式输入行动
  ↓
规则层检查条件、资源、时间、风险和地点
  ↓
结算成功 / 部分完成 / 拒绝 / 失败 / 待确认
  ↓
写入时间、资源、地点、关系、物品、事实和履历
  ↓
生成下一轮不重复的行动建议
  ↓
玩家继续生活
```

### 6.1 行动模式

| 模式 | 默认状态 | 行动入口 | 适用用户 |
|---|---|---|---|
| 选择优先 | 是 | 展示当前最合适的 3 个行动，隐藏输入框 | 想快速游玩的用户 |
| 丰富建议 | 否 | 展示完整行动池，按进度轮换顺序，可手动换顺序 | 想比较更多方向的用户 |
| 自由行动 | 否 | 展示行动卡，同时保留自由输入 | 喜欢主动探索的用户 |

模式设置保存在本地，并必须实际影响行动入口数量、排序或输入能力。

## 7. 功能需求

优先级说明：P0 为第一阶段必须完成，P1 为第二阶段优先，P2 为后续扩展。

### 7.1 应用壳与响应式 UI（P0）

- 桌面端提供侧栏导航。
- 手机端提供底部导航和可展开侧栏。
- 主体 UI 保持纸张、手账、自然色的原创视觉系统。
- UI 必须适配剧本主题色。
- 页面包含当前场景、角色、人物、履历、地图和设置。
- 行动入口在选择优先模式下首屏可见。
- 输入反馈、行动结果和资源变化必须清晰可见。
- 页面不应被大量永久信息卡片压缩成后台仪表盘。

状态：已完成基础版本，持续优化中。

### 7.2 剧本加载与切换（P0）

- 支持内置剧本。
- 支持导入 `.aiworld.json` 剧本包。
- 支持在不替换主体 UI 的情况下切换剧本。
- 剧本可定义标题、描述、主题色、人物、地点、行动和初始状态。
- 无效剧本必须被拒绝，并给出可理解的错误提示。

状态：内置剧本和基本导入入口已完成；更严格 schema 待开发。

### 7.3 角色创建（P0/P1）

第一阶段后续补充：

- 玩家姓名。
- 年龄或人生阶段。
- 身份和职业。
- 初始特质。
- 初始资源或背景选项。
- 剧本可控制字段，不允许玩家编辑规则不允许的字段。

角色创建结果需要写入当前剧本存档，并影响初始行动、关系、资源或叙事。

状态：当前原型使用剧本预设角色，待开发。

### 7.4 当前场景（P0）

- 显示当前地点、时间、季节、天气和氛围。
- 显示当前叙事内容。
- 显示“眼下的线索”或当前关注点。
- 显示本轮行动入口和每个行动的时间、精力、金钱、风险。
- 行动执行后显示结果状态和状态差异。

状态：已完成基础版本。

### 7.5 行动生成与去重（P1）

本地生成器必须在没有 AI 的情况下工作：

- 根据当前剧本行动池生成候选行动。
- 根据当前时间、地点、资源和最近行动调整候选。
- 记录每次已执行行动的 `actionId`。
- 默认排除最近一次已完成的规则行动。
- 为同一规则生成不同标题、描述或关注点的变体。
- 候选行动的显示 ID、规则 ID、标题和实际结算逻辑必须可追踪。
- 当候选不足时，可以回收旧规则，但必须使用新的变体并明确避免标题重复。

AI 生成接入后：

```text
剧本规则与当前状态
  ↓
AI 提供候选行动
  ↓
schema 校验与安全过滤
  ↓
重复检测与规则映射
  ↓
确定性规则结算
  ↓
生成叙事
```

状态：本地行动变体生成已开始，AI 候选生成待开发。

### 7.6 确定性行动结算（P0）

- 检查金钱、精力、时间、地点和前置条件。
- 成功行动推进世界状态。
- 部分完成允许产生收益和代价并存的结果。
- 条件不足时拒绝行动，不改变世界状态。
- 未识别的自由行动进入待确认状态，不得直接写入关键事实。
- 每次结算产生可读取的状态 diff。
- 规则层独立于 AI 叙事层。

### 7.7 NPC 与关系（P0/P1）

- 显示 NPC 姓名、身份、状态、摘要和关系值。
- 关系值只能由行动规则或明确事件改变。
- NPC 需要保留最近互动记录。
- 后续支持 NPC 自主事件和关系阈值事件。

状态：基础 NPC 和关系更新已完成；自主事件待开发。

### 7.8 时间、事件与履历（P0/P1）

- 行动消耗明确时间。
- 世界时间持续推进。
- 履历按时间倒序保存。
- 每条履历包含行动 ID、结果、文本和标签。
- 后续支持延迟事件、条件事件和事件链。

### 7.9 存档与数据迁移（P0）

- IndexedDB 保存运行时状态。
- 页面刷新后状态保持。
- 支持全部世界存档导出。
- 支持存档导入。
- 导入后状态和剧本一致。
- 未来版本必须保留存档版本号和迁移策略。

### 7.10 AI 配置与错误处理（P0/P1）

- 支持 OpenAI-compatible Endpoint、Model 和 API Key。
- API Key 只保存在本地原型环境，不由前端生产环境直接长期暴露。
- 未配置 API 时使用本地确定性模拟。
- 请求失败时显示可理解的错误和重试方式。
- 后续支持模型等级、调用额度、缓存和 BYOK。

### 7.11 PWA（P0）

- 支持安装为 Web App。
- 静态资源支持离线缓存。
- 离线时可以打开应用并使用本地规则。
- AI 请求失败不应破坏本地游戏状态。

## 8. UI 信息架构

### 8.1 当前场景

信息顺序：

1. 当前世界标题和时间。
2. 场景叙事。
3. 当前线索。
4. 行动入口。
5. 行动结果。
6. 辅助世界状态。

移动端选择优先模式将行动入口前置，保证玩家在首屏看到可操作内容；自由行动模式保留叙事优先和底部输入位。

### 8.2 角色

- 当前身份。
- 职业和年龄。
- 健康、精力、金钱、声望。
- 特质。
- 随身物品。

### 8.3 人物

- NPC 卡片。
- 关系值。
- 当前状态。
- 最近互动。

### 8.4 履历

- 时间线。
- 行动结果标签。
- 状态变化。
- 事件标签。

### 8.5 地图

- 已知地点。
- 当前地点。
- 地点类型和距离。
- 未探索区域不自动展示。

### 8.6 设置

- 行动呈现方式。
- 模型连接。
- 存档管理。
- 剧本导入。
- 当前剧本信息。

## 9. 数据与规则模型

### 9.1 核心状态

`GameState` 至少包含：

- `player`：角色基础信息、健康、精力、金钱、声望和特质。
- `world`：日期、时间、季节、天气、地点、氛围、叙事、当前线索和公共消息。
- `npcs`：人物状态和关系。
- `locations`：已知地点。
- `suggestedActions`：当前行动候选。
- `history`：履历和行动结果。
- `inventory`：物品。
- `knownFacts`：玩家已经知道的事实。
- `turn`：行动轮次。

### 9.2 行动对象

每个行动至少包含：

```ts
{
  id: string
  ruleId?: string
  title: string
  description: string
  location: string
  timeCost: number
  moneyCost: number
  staminaCost: number
  risk: string
  tone: string
}
```

其中：

- `id` 用于追踪当前显示的行动变体。
- `ruleId` 用于映射确定性结算逻辑。
- 成本和风险由规则层读取，不能只由叙事文本表达。

### 9.3 剧本包

当前 `.aiworld.json` 顶层包含：

- `manifest`
- `theme`
- `world`

后续扩展：

- `characterCreation`
- `actionPool`
- `rules`
- `conditions`
- `events`
- `npcSchedules`
- `uiExtensions`

剧本扩展必须保持向后兼容，并提供版本号和校验错误信息。

## 10. AI 使用边界

### 10.1 允许 AI 做的事

- 将自由文本解析为候选行动意图。
- 生成行动候选文案。
- 根据已经结算的状态 diff 生成叙事。
- 生成 NPC 对话和非事实性氛围描写。
- 给剧本作者提供内容草稿。

### 10.2 不允许 AI 直接做的事

- 直接增加金钱、物品、生命、关系或声望。
- 直接改变时间、地点或已知事实。
- 直接跳过行动条件。
- 直接创建未经验证的 NPC、地点或事件。
- 用一段叙事文本覆盖完整 `GameState`。

### 10.3 失败降级

当 AI 不可用、超时、额度不足或返回格式错误时：

- 保留确定性规则结算结果。
- 使用本地默认叙事。
- 显示模型请求失败，但不回滚已经合法结算的状态。
- 允许玩家重试叙事请求。

## 11. 开放核心与商业路线

### 11.1 开源部分

默认采用 Apache-2.0：

- 通用 UI 壳。
- 剧本读取和校验。
- 状态模型。
- 行动规划接口。
- 确定性规则引擎。
- 本地存档。
- API 适配接口。
- 示例原创剧本。

### 11.2 可商业化部分

- 官方 AI 托管。
- 跨设备同步。
- 剧本市场与分成。
- 高级剧本编辑器。
- 官方原创剧本包。
- 高级模型和长期记忆额度。
- 面向创作者和工作室的技术服务。
- 白标部署。

### 11.3 商业验证原则

第一阶段不急于收费或大规模投入，先验证：

- 玩家是否会持续经营同一个人生。
- 玩家是否会尝试多个剧本。
- 玩家是否愿意导入或创建剧本。
- 玩家愿意为什么功能付费。
- 每位活跃用户的模型成本是否可控。

原则上不采用影响游戏公平性的付费数值和强制广告。

## 12. 关键指标

### 12.1 游戏体验指标

- 首次进入到角色/剧本开始的完成率。
- 首次行动完成率。
- 首次行动后的第二次行动率。
- 单次游戏时长。
- 每个会话平均行动数。
- 次日和第七日回访率。
- 选择优先模式与自由行动模式的使用比例。
- 重复选项投诉率。

### 12.2 内容与创作指标

- 导入剧本数量。
- 用户创建剧本数量。
- 单个剧本平均游玩行动数。
- 用户主动分享剧本数量。

### 12.3 技术指标

- 行动规则错误率。
- AI 请求失败率。
- 状态 diff 校验失败率。
- 存档恢复失败率。
- 页面首屏加载时间。
- 移动端主要交互成功率。

## 13. 阶段计划

### 阶段 0：调研与决策（已完成）

- 调研 AI RPG、叙事引擎、NPC 模拟和剧本系统。
- 确认开放核心 + 官方服务路线。
- 确认原创剧本优先。
- 确认规则引擎优先于 AI 叙事。

### 阶段 1：通用 UI 与可玩原型（基本完成）

- 响应式 Web/PWA 应用壳。
- 当前场景、角色、人物、履历、地图和设置。
- 两个原创测试剧本。
- 剧本切换和主题替换。
- 选择优先、丰富建议、自由行动三档模式。
- 确定性行动结算。
- 行动结果和状态差异反馈。
- 本地存档、导入导出和 PWA。
- 桌面和移动端浏览器验证。

待补：角色创建、更严格剧本 schema、更多端到端测试。

### 阶段 2：行动生成与规则扩展（进行中）

当前已完成：

- 本地行动变体生成器。
- 最近行动记录。
- 最近行动排除。
- 行动 ID 与规则 ID 分离。
- 行动标题和候选去重。

下一步：

1. 定义更严格的行动候选 schema。
2. 增加状态条件和前置条件 DSL。
3. 建立候选行动的校验、去重和降级策略。
4. 将 AI 接入为候选生成器，而不是状态修改器。
5. 增加事件链、延迟事件和 NPC 自主行动。

### 阶段 3：剧本创作能力（计划）

- 角色创建配置。
- 剧本编辑器。
- 事件和规则可视化。
- 剧本预览和校验。
- 创作者导入、导出和版本管理。

### 阶段 4：封闭测试与商业验证（计划）

- 小范围邀请测试。
- 记录留存、行动数、成本和错误率。
- 访谈玩家和剧本作者。
- 验证托管 AI、同步、内容包和创作工具的付费意愿。

## 14. 验收标准

### 14.1 第一阶段验收

- 手机和桌面均可完整进入并游玩。
- 剧本切换不破坏主体 UI。
- 默认选择优先模式不依赖输入框即可推进。
- 自由行动模式可以恢复输入框。
- 不合理行动被规则层拒绝。
- 合法行动会消耗正确的时间和资源。
- AI 不可用时仍可继续运行。
- 页面刷新后存档仍存在。
- 导出和导入后状态一致。
- 每次行动都有可追踪的履历和结果。
- 成功、部分完成、拒绝、未知行动均有可理解反馈。
- 移动端底部导航、行动入口和安全区域正常。

### 14.2 第二阶段验收

- 连续行动后不会立即出现相同的行动标题。
- 行动候选 ID 唯一且可映射到规则 ID。
- 最近行动被记录并参与下一轮生成。
- 本地生成器无 API 也能产生新行动变体。
- AI 候选格式错误时不会污染 `GameState`。
- 条件不足、候选重复和生成失败都有降级方案。
- 规则结算和叙事生成可以独立测试。

## 15. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| AI 生成成本过高 | 商业不可持续 | BYOK、额度、缓存、模型分级、本地降级 |
| AI 修改事实 | 世界状态失真 | 规则层掌握事实，AI 只提交候选或叙事 |
| 行动内容重复 | 玩家快速流失 | actionId、ruleId、变体池、最近行动排除 |
| 剧本质量参差 | 用户体验不稳定 | schema、校验、预览、创作者工具 |
| 既有 IP 授权风险 | 无法商业发布 | 首发使用原创世界，社区剧本自行承担授权责任 |
| 移动端信息过载 | 操作困难 | 选择优先、首屏行动、抽屉和折叠信息层 |
| 本地存档损坏 | 用户数据丢失 | 版本号、导出、导入前校验和迁移策略 |
| 插件职责冲突 | 修改互相覆盖 | 单一主责 Skill、串行调用、项目规则管控 |

## 16. 插件与 Skill 调用规则

- 每个阶段只指定一个主责 Skill。
- 游戏 UI 与交互由 `game-ui-frontend` 主责。
- 游戏体验和浏览器验收由 `game-playtest` 主责。
- React/TypeScript 工程实现遵守 `build-web-apps:react-best-practices`。
- 纯技术故障才使用 `build-web-apps:frontend-testing-debugging`。
- 视觉资产只有在确实需要位图、插画或素材时才调用 `imagegen`。
- 同类型游戏设计、游戏 UI、游戏测试或资源生成工具禁止并行修改同一目标。
- 社区插件必须先核验来源、许可证、安装路径、权限和实际用途，再由用户明确同意后安装。
- 插件和 Skill 只服务开发流程，不进入游戏运行时依赖。
- `game-dev-studio` 已卸载，后续不得重新调用。

## 17. 当前项目文件映射

- [App.tsx](./src/App.tsx)：应用壳、页面和主要交互。
- [actionEngine.ts](./src/engine/actionEngine.ts)：确定性行动结算。
- [actionPlanner.ts](./src/engine/actionPlanner.ts)：行动呈现模式和排序。
- [suggestionEngine.ts](./src/engine/suggestionEngine.ts)：本地行动变体生成。
- [store.ts](./src/store.ts)：运行时状态、本地持久化和剧本切换。
- [types.ts](./src/types.ts)：核心数据类型。
- [scripts.ts](./src/data/scripts.ts)：内置原创剧本。
- [AGENTS.md](./AGENTS.md)：项目协作、架构和插件调用规则。

## 18. 待决策事项

以下事项在进入对应开发阶段前再确认：

- 角色创建是首次进入必选流程，还是允许使用剧本默认角色。
- 行动卡是否允许玩家主动锁定或收藏某个方向。
- AI 行动候选由单次请求生成，还是由本地候选池批量缓存。
- 第二阶段是否引入完整事件条件 DSL。
- 首个公开原创剧本的题材、长度和内容分级。
- 封闭测试规模、部署方式和数据采集方案。

