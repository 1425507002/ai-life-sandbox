# AI Life Worlds 项目协作规则

## 项目定位

这是一个本地优先的 AI 人生模拟器 Web/PWA 原型。核心原则是：确定性规则引擎掌握事实、资源和状态变化；AI 只负责行动解析辅助与叙事表达，不能直接凭空修改关键状态。

## 当前技术基线

- React + Vite + TypeScript
- Zustand 状态管理
- IndexedDB 本地存档
- PWA 静态壳
- OpenAI-compatible API 适配接口
- 原创 `.aiworld.json` 剧本包

## 工作流

- 涉及游戏规则、循环、人物关系、资源、节奏或体验时，使用项目文档、测试和现有前端技能进行审查；不依赖外部游戏开发 Skill。
- 涉及游戏 UI、HUD、菜单、叙事面板、输入反馈、移动端交互或游戏化信息层级时，优先使用 `game-ui-frontend`；它是本项目唯一的游戏 UI 专项评审入口。
- 涉及玩家流程、启动、主要行动、状态变化、截图验收或移动端可玩性时，使用 `game-playtest`；它是本项目唯一的游戏体验验收入口。
- 涉及 React/TypeScript 代码时，遵守 `build-web-apps:react-best-practices`。
- 涉及页面样式或新界面时，保持当前纸张、手账、自然色的原创视觉系统；不要退回模板化后台风格。
- 涉及纯技术问题（编译、控制台错误、定位浏览器回归）时，才使用 `build-web-apps:frontend-testing-debugging`；游戏体验验收优先由 `game-playtest` 负责。
- 完成行为变化前至少补充相应单元测试或浏览器交互验证，并明确记录测试结果。
- 行动入口默认采用选择优先；自由输入属于可选模式，不得成为唯一玩法。模式变化必须同时影响行动入口数量、排序或输入能力，并保持离线规则可玩。

## Skill 与插件调用管控

- 每个阶段只指定一个主责 Skill；先完成主责 Skill 的分析或产出，再进入下一阶段，禁止多个同类型 Skill 并行修改同一目标。
- 游戏 UI/交互设计阶段：`game-ui-frontend` 主责；`frontend-design` 只在需要补充品牌视觉或非游戏界面时串行调用，不与其同时改写同一套视觉方案。
- 游戏体验验收阶段：`game-playtest` 主责；`frontend-testing-debugging` 只处理技术故障，不与其重复执行同一轮验收。
- React 实现阶段：遵守 `build-web-apps:react-best-practices`；它提供工程约束，不重新定义游戏 UI 视觉方案。
- 视觉资产阶段：只有确实需要位图、插画或游戏素材时才调用 `imagegen`，并在 UI 方案确定后执行。
- 外部插件优先选择官方来源；社区插件必须先核验来源、许可证、安装路径、权限和实际用途，再由用户明确同意后安装。
- 禁止为了同一目标同时启用多个游戏设计、游戏 UI、游戏测试或资源生成类插件；如确需第二个工具，必须说明它与当前主责工具的边界，并串行执行。
- 插件/Skill 只服务开发流程，不进入游戏运行时依赖；项目代码必须在没有这些工具时继续构建、测试和运行。
- 每次调用前记录：目标、主责 Skill、输入、预期输出；每次调用后检查输出是否与目标一致，发现职责重叠时停止后续调用并重新选择主责工具。
- `game-dev-studio` 已卸载，后续不得按旧文档或缓存重新调用。

## 架构边界

- 不把 UI、行动结算、存档、AI 请求和剧本内容重新混成单一巨型文件。
- 新剧本优先通过 `ScriptPackage` 和 `.aiworld.json` schema 扩展，不复制整套页面。
- 所有状态变更都经过规则层；叙事文本不能成为状态事实来源。
- 第二阶段的行动生成采用“本地确定性生成器 + 可替换 AI 适配器”：本地生成器必须能独立产出不重复的行动入口，AI 只能提供候选内容，不能直接修改 `GameState`。
- 每次已执行行动都要记录 `actionId`；后续行动池至少排除最近一次规则行动，并保证候选 `id`、标题和规则映射可追踪。
- 第一阶段不引入账号、云同步、多人、复杂战斗或既有 IP 内容。

## 常用命令

```bash
pnpm dev
pnpm build
pnpm test
```

在本机 Codex Windows 沙箱的中文路径下，如果 esbuild 无法解析依赖链接，可将项目临时映射到盘符后执行测试；这只是环境 workaround，不是应用运行时要求。
