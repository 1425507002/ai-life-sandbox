# AI 模型接入验收清单

本清单用于真实 API Key 填入前后的分阶段验收。API Key 只允许由用户在网页设置页手动填写，不写入源码、Git、剧本包或聊天记录。

## 接入前（无网络调用）

- [ ] 未配置 Key 时，游戏仍可用本地确定性行动和叙事继续游玩。
- [ ] Endpoint、Model、Key 任一为空时，不发出请求并给出明确提示。
- [ ] 请求上下文只包含有界的记忆包，不发送完整存档或隐藏地点详情。
- [ ] AI 候选必须通过 JSON 解析、字段 schema、规则 ID 白名单、去重和人物/地图可见性校验。
- [ ] AI 不能直接修改 `GameState`；资源、时间、关系和地图发现必须由规则层结算。

## 连接测试

- [ ] HTTP 200 且存在可识别 completion 内容时显示成功和延迟。
- [ ] 401/403 分类为认证错误，保留服务商原文。
- [ ] 1305、429 或访问量过大分类为临时限流，保留服务商原文并允许重试。
- [ ] 1310、周限额、余额或额度不足分类为配额错误，不伪装成访问量过大。
- [ ] 408/504、AbortError 分类为超时；网络异常分类为网络错误。
- [ ] 非 JSON、无 choices 或无文本时分类为响应格式错误。
- [ ] 服务商有明确错误时，以服务商错误文本为主；本地分类只作为诊断元数据。

## 真实模型短流程

- [ ] 新建人生后生成至少一轮候选，候选 ID、规则 ID 和标题可追踪。
- [ ] 连续刷新候选不会重复上一轮已执行行动，也不会出现完全相同的候选池。
- [ ] 婴儿/儿童阶段不出现年龄不允许的行动、职业或地点。
- [ ] 未相遇 NPC 不进入人物面板；未发现地点不进入已知地图。
- [ ] 成功、失败、条件不足、拒绝、超时、限流和格式错误均能回退到本地可继续状态。
- [ ] AI 叙事与规则结算不一致时，以规则状态和状态差分为准。
- [ ] 刷新页面、导出导入存档后，状态、行动记录和记忆游标一致。

## 记录要求

每次真实测试只记录：模型/端点标识、请求耗时、HTTP/业务错误码、输入行动、候选 ID、结果类型、状态差分和 token 用量。禁止记录完整 API Key。
